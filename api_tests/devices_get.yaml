# Test generated by RoostGPT for test api-gen-2 using AI Type Vertex AI and AI Model text-bison

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - scenario: Successful API request
    payload: {}
    headers: {}
    expect:
      - statusCode: 200
      - hasProperty: data

  - scenario: API request with validation failure
    payload: {}
    headers: {}
    expect:
      - statusCode: 400
      - hasProperty: error

  - scenario: API request with edge case
    payload: {}
    headers: {}
    expect:
      - statusCode: 200
      - hasProperty: data
